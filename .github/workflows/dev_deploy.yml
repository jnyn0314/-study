# 개발용 CI/CD 스크립트
# 작성자 : 정여진
# 작성일 : 2025.06.19.
name: UMC-WEEK11-WORKBOOK

on:
  pull_request:
    types: [closed]
  workflow_dispatch: # (2).수동 실행도 가능하도록

jobs:
  build:
    runs-on: ubuntu-latest # (3).OS환경
    if: github.event.pull_request.merged == true && github.event.pull_request.base.ref == 'develop'